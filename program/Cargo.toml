[package]
name = "kswap-program"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
pinocchio = "0.9"
pinocchio-system = "0.3"
pinocchio-token = "0.4"

[dev-dependencies]
solana-pubkey = "2.2"

